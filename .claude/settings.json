{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(jq -r '.tool_input.command // empty'); echo \"$cmd\" | grep -qE '(git commit|npm run build)' && cd /home/user/ColdMailAI && npx vercel --prod 2>&1 || true",
            "timeout": 120,
            "statusMessage": "Deploying to Vercel..."
          }
        ]
      }
    ]
  }
}
